Из того что сделал:
1) Верстка. Не скажу что идеально, немного торопился, есть косяки. Адаптацию под разрешения меньше 1600 пикселей не делал.
2) ВЕСЬ заданный функционал задания реализовал. Ничего сложного не увидел здесь.

Проблемы по апи:
1) Урл получения списка тасков отличается от стандарта всех остальных урлов. 
Т.е вместо /api/{tenantguid}/Tasks , используется /odata/tasks где тенант надо передавать в параметрах что не верно.
2) Почему нету отдельного метода для добавления комментария, вынужден использовать метод /api/{tenantguid}/Tasks
для этого. В принципе добавление комментария проблем не вызывает.
3) Из-за обновления параметров таска, создается вместе с обновлением и пустой комментарий. Добавил в реализацию фильтр для таких комментарий.
4) Почему у пользователей (в комментариях) параметр userName имеет значение айдишника вместо имени? Это не правильно.

Что не реализовывал (в целях экономии времени):
1) Спиннер загрузки данных.
2) Отдельный сервис обработки ошибок апи.



В случае отрицательного решения, как минимум надеюсь на грамотный код ревью. Спасибо.